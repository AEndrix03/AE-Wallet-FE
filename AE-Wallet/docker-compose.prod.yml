version: "3.8"
services:
  ae-wallet-fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ae-wallet-fe
    ports:
      - "81:81"
    networks:
      - ae-wallet-fe

networks:
  ae-wallet-fe:
    driver: bridge
